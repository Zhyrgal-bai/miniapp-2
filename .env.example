# Copy to .env and fill in. See README deploy notes.
# Обязательно sslmode=require для управляемого Postgres (Render DB, Neon и т.д.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Шифрование Business.botToken в БД (AES-256-GCM). На production/Render обязательно: ≥32 символа случайной строки.
# Локально без ключа токены хранятся plain + botTokenHash (только для dev).
BOT_TOKEN_SECRET_KEY=

# Public HTTPS URL of this API (no trailing slash), e.g. https://miniapp-2-qipd.onrender.com.
# Render: задайте тот же URL, что у Web Service; build вшивает его в SPA через RENDER_EXTERNAL_URL.
API_URL=

# Mini App в Telegram (кнопки бота): приоритет MINI_APP_URL → FRONTEND_URL → FRONT_URL → PUBLIC_URL.
# Должен совпадать с хостом, где открывается /merchant (часто тот же URL, что API_URL).
# MINI_APP_URL=https://miniapp-2-qipd.onrender.com
# FRONT_URL=https://miniapp-2-qipd.onrender.com

# Official Finik Acquiring — тестовый аккаунт платформы (Render ENV, не БД магазинов):
# PEM: одна строка с \n или многострочный secret в Render Dashboard
# FINIK_PRIVATE_KEY=
# FINIK_PUBLIC_KEY=
# FINIK_API_KEY=
# FINIK_ACCOUNT_ID=

# ARCHA SaaS subscription billing (Official Finik RSA — platform account):
# callback POST {API_URL}/api/platform/subscription-finik-webhook
# FINIK_API_KEY=
# FINIK_ACCOUNT_ID=
# FINIK_PRIVATE_KEY=
# FINIK_PUBLIC_KEY=
# Optional alias (fallback if FINIK_* unset):
# PLATFORM_FINIK_API_KEY=
# PLATFORM_FINIK_ACCOUNT_ID=
# Deprecated — Official Acquiring does not use shared secret:
# PLATFORM_FINIK_SECRET=
# Legacy: старые платежи подписки через Finik магазина — POST /api/payments/finik-webhook
# Опционально: FINIK_API_BASE_URL, FINIK_API_CREATE_PAYMENT_PATH, FINIK_USE_MOCK, FINIK_WEBHOOK_SIGNATURE_HEADER — см. finikMerchant.ts.
# Phase 3 scaffold (create payment router; checkout пока через finikMerchant): legacy | official | auto
# FINIK_CREATE_API_MODE=legacy
# FINIK_OFFICIAL_ACQUIRING_BASE_URL=https://beta.api.acquiring.averspay.kg
# Optional alias for FINIK_OFFICIAL_ACQUIRING_BASE_URL:
# FINIK_API_URL=https://beta.api.acquiring.averspay.kg
# Platform-managed merchants: магазины хранят только finikAccountId; подпись и API Key — в ENV ARCHA
# FINIK_PLATFORM_MANAGED_MERCHANTS=1
# См. docs/integrations/finik-platform-managed-architecture.md
# FINIK_OFFICIAL_ACQUIRING_CREATE_PATH=/v1/payment
# FINIK_OFFICIAL_ACQUIRING_STATUS_PATH=/v1/payment/{paymentId}
# FINIK_API_GET_PAYMENT_PATH=/payments/{id}
# FINIK_RSA_PRIVATE_KEY=   # PEM для official API (позже)
# FINIK_RSA_PRIVATE_KEY_PATH=
# См. docs/integrations/finik-acquiring.md
# BASE_URL=
# Обязательно на production/Render: secret_token для setWebhook; Telegram шлёт X-Telegram-Bot-Api-Secret-Token (1–256 символов [A-Za-z0-9_-]).
TELEGRAM_WEBHOOK_SECRET=
# Лимит POST вебхука Telegram с одного IP в секунду (по умолчанию 80).
TELEGRAM_WEBHOOK_MAX_RPS_PER_IP=80
# Только если нужен старый URL /webhook/<числовой_business_id> на переходный период (по умолчанию выкл.).
# TELEGRAM_WEBHOOK_LEGACY_NUMERIC_PATH=1

# SaaS-боты: URL вида POST {API_URL}/webhook/<32_hex из Business.webhookRouteToken>; env-боты: …/telegram-webhook/{index}.

# Telegram bots: several tokens for several stores (same order as BOT_OWNER_IDS / CHAT_IDS).
# Example: BOT_TOKENS=123:AAA,456:BBB
BOT_TOKENS=
# Single-bot legacy (if BOT_TOKENS is empty)
# BOT_TOKEN=

# DB user id владельца магазина i = i-й бот в BOT_TOKENS (для уведомлений и API бота).
BOT_OWNER_IDS=
# Чат админа i-го магазина (опционально, тот же порядок что и токены).
CHAT_IDS=

# Telegram user IDs allowed in admin (comma-separated).
# Регистрация магазина всегда через заявку и ручной approve (Telegram-кнопки или /platform-admin).
ADMIN_IDS=
# Явный список операторов платформы (если пусто, используется ADMIN_IDS как fallback).
PLATFORM_OPERATOR_IDS=
# bcrypt hash пароля оператора (обязательно для secure operator mode).
# Пример генерации: node -e "import bcrypt from 'bcryptjs'; bcrypt.hash('your-password', 12).then(console.log)"
OPERATOR_PASSWORD_HASH=
# TTL unlock-сессии оператора (минуты, диапазон 15..30).
OPERATOR_SESSION_TTL_MIN=20
# Окно повторной верификации для dangerous actions (минуты).
OPERATOR_REAUTH_WINDOW_MIN=5
# УСТАРЕЛО: plain-text пароль панели.
# ADMIN_PANEL_PASSWORD=

# Mini App `/platform` и `/platform-admin`: авторизация по заголовку `x-telegram-init-data`.
# Подпись считается для КАЖДОГО бота отдельно: сервер пробует PLATFORM_WEBAPP_BOT_TOKEN, затем каждый токен из BOT_TOKENS и BOT_TOKEN.
# Либо откройте Web App через ссылку с start_param = id магазина или shop_<id>, тогда возьмётся токен этого Business из БД.
# Mini App с разных SaaS-ботов: по умолчанию перебираются токены до 1000 Business в БД (отключить: WEBAPP_VALIDATE_INITDATA_SCAN_STORE_BOTS=0).
# WEBAPP_VALIDATE_INITDATA_SCAN_STORE_BOTS=
# Порядок проверки initData: start_param → токен этого магазина в БД → BOT_TOKENS / BOT_TOKEN → скан магазинов в БД.
# Лог первых 10 символов токена при каждой попытке в dev или если TELEGRAM_INIT_DEBUG=1
# TELEGRAM_INIT_DEBUG=
# Локально без Telegram: NODE_ENV=development + DEV_TELEGRAM_USER_ID=ваш_telegram_id + пустой initData → пропуск проверки подписи WebApp.
# На staging при NODE_ENV=production: SKIP_TELEGRAM_WEBAPP_AUTH=1 и тот же DEV_TELEGRAM_USER_ID (временно).
# PLATFORM_WEBAPP_BOT_TOKEN=
# Админ-эндпоинты дополнительно проверяют ADMIN_IDS.
# GET /api/platform/admin/businesses; POST /api/platform/admin/disable; POST /api/platform/admin/extend;
# POST /api/platform/admin/purge-business (body: { businessId }) — полное удаление магазина из БД;
# POST /api/platform/admin/block и /unblock (body: { businessId }).

# Phase 18 — security / production hardening
# CORS allowlist (comma-separated origins). Default `*` preserves backward compatibility.
# CORS_ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-api.example.com
# Telegram initData freshness (seconds, default 86400 = 24h)
# TELEGRAM_INITDATA_MAX_AGE_SEC=86400
# Optional replay guard (in-memory; use Redis for multi-instance later)
# TELEGRAM_INITDATA_REPLAY_GUARD=1
# Trust reverse proxy for rate limits / client IP (set when behind nginx/Caddy)
# TRUST_PROXY=1
# Enable HSTS header when TLS terminates at reverse proxy
# TRUST_HTTPS=1
# Request timeout for non-webhook routes (ms, default 30000)
# REQUEST_TIMEOUT_MS=30000
# Telemetry client-error rate limit per minute
# TELEMETRY_RATE_LIMIT_PER_MIN=30
# Cloudinary (required together for merchant/support/receipt uploads)
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=

# Лимиты API: общий число запросов с одного IP в минуту под `/api/`, строже для части платформенных POST.
# API_RATE_LIMIT_PER_MIN=60
# API_RATE_LIMIT_STRICT_PER_MIN=10
# Размер JSON-тела: по умолчанию 10kb для API; Telegram/Finik webhook — см. middleware/jsonBodyLimits.ts
# API_JSON_BODY_LIMIT=10kb
# API_JSON_BODY_LIMIT_LARGE=512kb

# FINIK_PUBLIC_KEY=

# Platform-managed Yandex Delivery (offers MVP — no merchant tokens)
# YANDEX_DELIVERY_OAUTH_TOKEN=
# YANDEX_DELIVERY_API_BASE=https://b2b.taxi.yandex.net
# YANDEX_DELIVERY_USE_MOCK=1
# YANDEX_DELIVERY_TIMEOUT_MS=15000
# YANDEX_DELIVERY_HTTP_MAX_RETRIES=2
# YANDEX_DELIVERY_HTTP_RETRY_BASE_MS=500
# YANDEX_DELIVERY_OFFERS_PATH=/b2b/cargo/integration/v2/offers/calculate
# YANDEX_DELIVERY_CLAIMS_ENABLED=1
# YANDEX_DELIVERY_CLAIMS_CREATE_PATH=/b2b/cargo/integration/v2/claims/create
# YANDEX_DELIVERY_CLAIMS_ACCEPT_PATH=/b2b/cargo/integration/v2/claims/accept
# YANDEX_DELIVERY_OFFER_CACHE_TTL_MS=1800000
# YANDEX_DELIVERY_CLAIMS_INFO_PATH=/b2b/cargo/integration/v2/claims/info
# YANDEX_DELIVERY_WEBHOOK_BASE_URL=https://your-host/api/delivery/providers/yandex/webhook?
# YANDEX_DELIVERY_WEBHOOK_SECRET=

# Phase 5: delivery recovery (missed webhooks, retries, dead-letter)
# DELIVERY_RECOVERY_ENABLED=1
# DELIVERY_RECOVERY_INTERVAL_MS=120000
# DELIVERY_RECOVERY_STALE_MS=300000
# DELIVERY_RECOVERY_BATCH_SIZE=50
# DELIVERY_RECOVERY_MAX_ATTEMPTS=8
# DELIVERY_RECOVERY_RETRY_BASE_MS=30000
# DELIVERY_RECOVERY_RETRY_MAX_MS=3600000

# Интервал фоновой задачи напоминаний/автоотключения по подписке (мс). По умолчанию 86400000 (24 ч).
# Минимум 60000. См. `runSubscriptionMaintenanceOnce` в src/server/subscriptionMaintenance.ts.
# SUBSCRIPTION_CRON_MS=86400000

# Phase 13: Free Usage Model (5 paid orders before subscription). Set 0/false/off to disable.
# ARCHA_FREE_USAGE_MODEL=1
# ARCHA_FREE_ORDERS_LIMIT=5

# (устарело) SAAS_MANUAL_APPROVAL — авто-создание магазина отключено; переменная игнорируется.
# SAAS_MANUAL_APPROVAL=
# Fallback: один чат для уведомлений, если CHAT_IDS не задан
CHAT_ID=

# Mini App (HTTPS, без финального slash). Приоритет: MINI_APP_URL, затем FRONTEND_URL, FRONT_URL, PUBLIC_URL.
# MINI_APP_URL=
# FRONTEND_URL=
FRONT_URL=
