/** @deprecated Import from `./telegramSession` — kept for backward-compatible imports. */
export {
  hasVerifiedTelegramInitData,
  waitForTelegramInitData,
  type WaitForInitDataOptions,
} from "./telegramSession";

export type { WaitInitDataResult } from "./telegramSession";
